@@ .. @@
 import { regions, type Region } from '../lib/regions';
 
-export default function EmergencySupport({ region }: { region: Region }) {
}
+export function EmergencySupport({ region }: { region: Region }) {
}