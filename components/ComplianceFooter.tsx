@@ .. @@
 import { regions, type Region } from '../lib/regions';
 
-export default function ComplianceFooter({ region }: { region: Region }) {
}
+export function ComplianceFooter({ region }: { region: Region }) {
}